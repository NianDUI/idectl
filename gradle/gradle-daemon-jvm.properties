#This file pins the Gradle daemon JVM so the build always runs on JDK 21.
#Gradle 9 does not support JDK 26 (the machine default), which otherwise fails with a cryptic "26".
#Gradle auto-detects an installed JDK 21 (e.g. Temurin 21) — no manual JAVA_HOME needed.
toolchainVersion=21
